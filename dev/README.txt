VS
  This is VisualStudio solution that allows 1) building the JVM and executng it as a Console application; this helps debugging a lot. 2) build util files. Particularly, veriy_struct.c makes sure lenght of Java structures is as needed in order not to crash the JVM.

test
  Some Java sources to test the JVM.